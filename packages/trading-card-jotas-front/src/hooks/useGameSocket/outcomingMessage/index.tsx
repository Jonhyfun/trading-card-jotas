const saveDeck = async (deck: string[], withMessage = false) => {};

const placeCard = async (card: string) => {};

const joinRoom = async (room: string) => {};

const leaveRoom = async (room: string) => {};

const fetchHand = async () => {};

export { saveDeck, placeCard, joinRoom, leaveRoom, fetchHand };
