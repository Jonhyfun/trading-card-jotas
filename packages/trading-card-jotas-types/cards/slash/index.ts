import type { CardType } from "../types";

const Card: CardType = {
  label: "/",
  value: null,
  operation: "/",
  desc: "Divide o número anterior com a próxima carta numérica.",
  limit: 2,
  effect: () => {},
};

export default Card;
